# Server Port & SSL (HTTPS - Let's Encrypt 직접 등록)
server.port=${SERVER_PORT:8080}
server.ssl.enabled=true
server.ssl.certificate=/etc/letsencrypt/live/quietjun.cloud/fullchain.pem
server.ssl.certificate-private-key=/etc/letsencrypt/live/quietjun.cloud/privkey.pem

# Database Configuration (MySQL - Production)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/ssafy_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
spring.datasource.username=${DB_USERNAME:quietjun}
spring.datasource.password=${DB_PASSWORD:dmstj@0205M}

# JPA Production Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
