# Server Port
server.port=8080

# Include Local Secret Configuration if available (Ignored by Git)
spring.config.import=optional:classpath:application-local.properties

# Database Configuration (MySQL - Dev Local)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/ssafy_db?serverTimezone=Asia/Seoul}
spring.datasource.username=${DB_USERNAME:quietjun}
spring.datasource.password=${DB_PASSWORD:dmstj@0205M}

# JPA  Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
